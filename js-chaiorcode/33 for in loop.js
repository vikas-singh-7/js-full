var obj ={
    name:"vikas",
    age:22,
    lname:"singh"
};

for(var key in obj){
    console.log(key + " " + obj[key]);

}

// here in for in loop we use for(var key in object) 
// means the baribale key is going to add the propertyes of obj

// obj[property/name] = value of object
