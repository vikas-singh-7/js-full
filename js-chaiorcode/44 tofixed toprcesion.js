// to fixd is usd whene w hav a long imal numbr and we want to makee it a fixed decimal son we use to fixed 

var a = 10.873;

var b = a.toFixed(2);   // 10.87   as we say after decial only take two values remove the rest

console.log(b);   

// toPecision()   // it is used  to roundoff

var c = 10.782;

var h = c.toPrecision(2); //  11 as 
console.log(h)
