// simply i w ehahv a lot of space in starting or ening in an string the unwanting space we can remove it using trim

var str="     hey nigga    ";   // space in staringa nd end

console.log(str);

var a = str.trim();   // removes the extra or unwanting space in the end or beginnening of a string 

console.log(a)
